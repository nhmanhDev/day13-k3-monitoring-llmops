# ==============================================================================
# Root requirements — tất cả dependencies cho toàn bộ repo
# Cài: pip install -r requirements.txt
# ==============================================================================

# --- Structured Logging (structured-logs.py) ---
structlog

# --- PII Scrubbing (pii-scrubbing.py) ---
presidio-analyzer
presidio-anonymizer
spacy

# --- OpenTelemetry + Jaeger (opentelemetry/) ---
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-requests

# --- Langfuse Integration (langfuse/) ---
langfuse
python-dotenv

# --- Shared (FastAPI server cho OTEL + Langfuse modules) ---
fastapi
uvicorn
